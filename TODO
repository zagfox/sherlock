6. client could use it in thread

5. lock lease, server send check(ok), client also need check connection

4. after transfer data, reply log

fixed, 3. paxos too slow, add random time heart beat

fixed, test script error. 2. there are 5 servers, 2 is running, not forming a view. start 3 together, get stuck

fixed 1. restart server, client call rpc. Result in "too many open files"
